// Tree-sitter-only corpus.
//
// These fixtures pin tree-sitter's error-recovery shape for the
// context-sensitive render-rules statements (render:, show_and_return:, when).
// The JS parser is a hand-rolled recursive-descent parser and its
// recovery legitimately diverges from tree-sitter's LR-core reduction
// on malformed input, so these cases are excluded from the JS-side
// corpus/parity/roundtrip suites, which read the flat `test/corpus/`
// directory. Tree-sitter's own `tree-sitter test` runner walks
// `test/corpus/` recursively, so these still run there.
//
// See PR #491 review discussion under comment 6338583.

================
Error: render with no value
================
subagent main:
    reasoning:
        actions:
            list_it: @actions.list
                when @connection.messaging
                    render:
---

(ERROR
  (key
    (id)
    (id))
  (key
    (id))
  (key
    (id))
  (key
    (id))
  (expression_with_to
    expression: (expression
      (member_expression
        (expression
          (atom
            (at_id
              (id))))
        (id))))
  (expression
    (member_expression
      (expression
        (atom
          (at_id
            (id))))
      (id))))

================
Error: show_and_return with no value
================
subagent main:
    reasoning:
        actions:
            list_it: @actions.list
                when @connection.messaging
                    render: @response_formats.card
                        show_and_return:
---

(ERROR
  (key
    (id)
    (id))
  (key
    (id))
  (key
    (id))
  (key
    (id))
  (expression_with_to
    expression: (expression
      (member_expression
        (expression
          (atom
            (at_id
              (id))))
        (id))))
  (expression
    (member_expression
      (expression
        (atom
          (at_id
            (id))))
      (id)))
  (expression
    (member_expression
      (expression
        (atom
          (at_id
            (id))))
      (id))))
